-----------------------------


I want to add a new feature to the session screen. I want to add the total elapsed session time.
- The total elapsed session time is the time since session started.
- The format will be HH:MM:SS.
- The total time doesn't increase while in pause.

Rules:
- The coder agent is in charge of the implementation, and will always ask the architect agent for plan first.
- The plan and progress should be available in a text file, so the work can be resumed if interrupted.
- The final solution should be kept as simple as possible

Be extremely concise. Sacrifice grammar for the sake of concision.


-----------------------------


I want to modify the appereance of the session screen.
The only elements in the session screen should be:
- The heart rate. The value only, not the string "beats per minute".
- The session buttons:
  - Start button
  - Pause/Resume button
  - Stop button
The rest of elements should be removed from session screen.

Rules:
- The coder agent is in charge of the implementation, and will always ask the architect agent for plan first.
- The plan and progress should be available in a text file, so the work can be resumed if interrupted.
- The final solution should be kept as simple as possible

Be extremely concise. Sacrifice grammar for the sake of concision.


-----------------------------


I want to add a new feature. The app will have two screens, so we have to split the already implementd logic. We don't change the logic only the visuals.
- The first screen.
  - This screen is the one visible when opening the app.
  - This is the configuration screen.
  - This screen contains:
    - The heart rate monitor conection logic.
    - The GPS status and logic.
    - A "New Session" button to start a new session.
  - When the "New Session" button is pressed the app will go to the second screen, the session screen.
- The second screen.
  - This screen is the session screen.
  - This screen contains the session logic, records logic and buttons. The behaviuor doesn't change.
  - When the "Stop" button is pressed, apart from all the Stop logic, the app will go to the configuration screen.

Rules:
- The coder agent is in charge of the implementation, and will always ask the architect agent for plan first.
- The plan and progress should be available in a text file, so the work can be resumed if interrupted.
- The final solution should be kept as simple as possible

Be extremely concise. Sacrifice grammar for the sake of concision.


-----------------------------


The manual test has failed.
- The GPS data has been updated only in the first 5 records, the rest keep the same value, but the runner was moving.
- I have uploaded the test file, look at it and analyze the possible cause.
- The test file: docs/Run_2026-09-12_16-54-43.gpx

Rules:
- The coder agent is in charge of the implementation, and will always ask the architect agent for plan first.
- The plan and progress should be available in a text file, so the work can be resumed if interrupted.
- The final solution should be kept as simple as possible

Be extremely concise. Sacrifice grammar for the sake of concision.


-----------------------------


I want to add a new feature. It should be possible to record a new running session.
- The app will act as a state machine with 3 states: WAITING_TO_START, RECORDING, PAUSE.
- When the app is in WAITING_TO_START state:
  - The needed recording variables are initialized.
- When the app is in RECORDING state:
  - The app records the heart rate readings and GPS location data very 1 second. This records reside on RAM.
  - The app writes on a permament resource, like a database or file system, the records. This happens every 30 seconds. This is done in order to not loose data in case of breakdown of the app and to limit the RAM usage.
- When the app is in PAUSE state:
  - The recording is paused temporary.
  - If the "RESUME" button is pressed:
    - The recording resumes.
  - If the "STOP" button is pressed:
    - All recorded data for that sesion is saved in a file.
    - The file has extension .gpx.
    - The file name needs to be unique. You can use the date for this.
    - The format for the content is the gpx format. An example of this format can be found on docs/Night_Run.gpx
    - The file has to be saved in a location easy available for the user to retrieve via USB.

Rules:
- The coder agent is in charge of the implementation, and will always ask the architect agent for plan first.
- The plan and progress should be available in a text file, so the work can be resumed if interrupted.
- The final solution should be kept as simple as possible

Be extremely concise. Sacrifice grammar for the sake of concision.


-----------------------------


I want to add a new feature. It should be possible to record a new running session.
- The app will act as a state machine with 3 states: WAITING_TO_START, RECORDING, PAUSE.
- When the app is in WAITING_TO_START state:
  - The button will display "START".
  - If the button is pressed the state will transition to RECORDING.
- When the app is in RECORDING state:
  - The button will display "PAUSE".
  - If the button is pressed the state will transition to PAUSE.
- When the app is in PAUSE state:
  - The button will be splitted in two buttons. One displays "RESUME" the other on displays "STOP".
    - If the "RESUME" button is pressed the state will transition to RECORDING.
    - If the "STOP" button is pressed the state will transition to WAITING_TO_START.

Rules:
- The coder agent is in charge of the implementation, and will always ask the architect agent for plan first.
- The plan and progress should be available in a text file, so the work can be resumed if interrupted.
- The final solution should be kept as simple as possible

Be extremely concise. Sacrifice grammar for the sake of concision.


-----------------------------

  - The app starts recording the heart rate readings and GPS location data very 1 second.
  - Every 30 seconds the recorded data is saved in disk or database

-----------------------------


I want to add a new feature to our runner app. The app will use the mobile phone GPS.
- The app will get the GPS data periodically, with an update rate of 1 second.
- The app will show the actual signal strenght og the GPS signal. It will be shown as 3 values: No signal, weak signal, good signal.
- The app will show the actual position coordinates from the GPS. 

Rules:
- The coder agent is in charge of the implementation, and will always ask the architect agent for plan first.
- The plan and progress should be available in a text file, so the work can be resumed if interrupted.
- The final solution should be kept as simple as possible

Be extremely concise. Sacrifice grammar for the sake of concision.


-----------------------------


Build the application described in requirements file following the rules
